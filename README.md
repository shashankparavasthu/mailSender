# mailSender
Bulk E-mail sender using python

## usage
- the first column in `.xlsx` should be the recepient email
- enclose the column field in your excel sheets with `$` when entering in the email-body/subject
- run: <br>
`python mail_sender.py`
